/**
 * DAG 连接器配置
 * 统一管理边的连接器（贝塞尔曲线）配置
 */

// ============================================
// 连接器配置
// ============================================
export const DAG_CONNECTOR_CONFIG = {
  /**
   * 连接器名称
   */
  name: 'dag-connector',

  /**
   * 总线在节点附近保持的最小直线距离，避免过于尖锐
   */
  minBusGap: 36,

  /**
   * 折线的额外 padding，用于创造“总线外延”效果
   */
  elbowPadding: 24,

  /**
   * 拐角圆角半径
   */
  cornerRadius: 12,
};

// ============================================
// 默认导出
// ============================================
export default DAG_CONNECTOR_CONFIG;
