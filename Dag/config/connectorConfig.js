/**
 * DAG 连接器配置
 * 统一管理边的连接器（贝塞尔曲线）配置
 */

// ============================================
// 连接器配置
// ============================================
export const DAG_CONNECTOR_CONFIG = {
  /**
   * 连接器名称
   */
  name: 'dag-connector',

  /**
   * 最小控制距离
   * 避免节点过近时曲线太扁
   */
  minControl: 50,

  /**
   * 控制点距离系数
   * 控制曲线弯曲程度（0-1之间，越大越弯曲）
   */
  controlFactor: 0.5
}

// ============================================
// 默认导出
// ============================================
export default DAG_CONNECTOR_CONFIG

